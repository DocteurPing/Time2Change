[package]
name = "api"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
tokio.workspace = true
application = { path = "../application" }
shared = { path = "../shared" }
