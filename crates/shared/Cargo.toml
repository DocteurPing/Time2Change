[package]
name = "shared"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
tracing-subscriber.workspace = true

[dev-dependencies]
# Only needed to emit through the subscriber under test.
tracing.workspace = true

[lints]
workspace = true
